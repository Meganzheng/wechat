
<form>
<i-panel title="">
    <i-input value="{{ value1 }}" title="联系人" autofocus placeholder="名字" />
    <i-input value="{{ value2 }}" type="number" title="手机号" placeholder="请输入手机号" />
    <i-input value="{{ value3 }}" type="textarea" title="详细地址" placeholder="请输入详细地址(最多50字)" maxlength="50" />
    <i-input value="{{ value4 }}" title="门牌号" placeholder="例：1单元101号"/>
</i-panel>
<button class='delete'>保存地址</button>
<button class='saveaddress' bindtap="itshow"> 删除地址</button>
<i-modal title="" visible="{{visible2}}"  bind:ok="handleClose2" bind:cancel="handleClose2">
    <view class='man'>确认要删除地址吗？</view>
</i-modal>
</form>